name: stamp_folder_widget_v2
description: "A reusable Flutter widget package for rendering a frosted stamp folder artwork."
repository: https://github.com/ryanByte1024/stamp_folder_widget_v2.git
version: 0.1.2

topics:
  - flutter
  - widgets
  - stamp-folder

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
